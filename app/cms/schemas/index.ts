/**
 * schemas/index.ts
 * Export all schema types for Sanity Studio
 *
 * Add your schema imports here and include them in the schemaTypes array.
 */

import { exampleSchema } from './example';

export const schemaTypes = [
  exampleSchema,
  // Add more schemas here as needed
];
