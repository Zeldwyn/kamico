/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}'],
  // In Tailwind v4, colors are defined in @theme in CSS
  // This config file is for content paths and plugins only
  plugins: [],
};
